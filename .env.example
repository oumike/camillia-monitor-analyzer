# Browser-facing API base. Keep this relative when the app and API share an origin.
VITE_API_BASE_URL=/api

# Local Vite proxy target. The ingestor defaults to this address.
VITE_DEV_API_PROXY=http://localhost:3000